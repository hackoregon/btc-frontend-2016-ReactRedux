export default function fixAnyLongLabel(label){
  const pacPattern = /pac|Political Action Committee/i;
  const filerPattern = /\ \(/;
  const extrasPattern = /-| /
  switch (label) {
    case expression:

      break;
    default:
  }
}