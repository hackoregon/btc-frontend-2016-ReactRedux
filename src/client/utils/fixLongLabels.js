export default function fixAnyLongLabel(label){
  var pacPattern = /pac|Political Action Committee/i;
  var filerPattern = /\ \(/;
  var extrasPattern = /-| /  
  switch (label) {
    case expression:

      break;
    default:

  }

}